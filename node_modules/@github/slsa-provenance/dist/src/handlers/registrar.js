"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.activeHandler = exports.clear = exports.register = void 0;
// Collection of all registered handlers. As new handlers are imlemented they
// should be registered via the register function.
const handlers = [];
/**
 * Registers a new handler for generating provenance statements.
 *
 * @param handler ProvenanceHandler instance to register
 */
function register(handler) {
    handlers.push(handler);
}
exports.register = register;
function clear() {
    handlers.splice(0, handlers.length);
}
exports.clear = clear;
/**
 * Returns the first registered handler that is enabled.
 *
 * @returns the active ProvenanceHandler instance, or undefined if none is
 * active
 */
function activeHandler() {
    return handlers.find((h) => h.enabled());
}
exports.activeHandler = activeHandler;
