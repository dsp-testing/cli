"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.processArgv = void 0;
const fs_1 = __importDefault(require("fs"));
const crypto_1 = __importDefault(require("crypto"));
const index_1 = require("../index");
async function cli(args) {
    const buffer = fs_1.default.readFileSync(args[0]);
    const algo = 'sha512';
    const digest = crypto_1.default.createHash(algo).update(buffer).digest('hex');
    const bundle = await (0, index_1.generateProvenance)({
        name: args[0],
        digest,
        algorithm: algo,
    });
    console.log(JSON.stringify(bundle));
}
async function processArgv() {
    try {
        await cli(process.argv.slice(2));
        process.exit(0);
    }
    catch (e) {
        console.error(e);
        process.exit(1);
    }
}
exports.processArgv = processArgv;
