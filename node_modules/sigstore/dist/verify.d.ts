/// <reference types="node" />
/// <reference types="node" />
import { KeyLike } from 'crypto';
import { SigstoreDSSEBundle } from './bundle';
import { Rekor } from './client';
export interface VerifyOptions {
    rekor: Rekor;
}
export declare class Verifier {
    private rekor;
    constructor(options: VerifyOptions);
    verify(payload: Buffer, signature: string, certificate: KeyLike): Promise<boolean>;
    verifyDSSE(bundle: SigstoreDSSEBundle): Promise<boolean>;
    private lookupCertificate;
}
//# sourceMappingURL=verify.d.ts.map